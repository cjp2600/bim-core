$iblock = new Bim\Db\Iblock\IblockIntegrate();
        $response = $iblock->Add(array(
            "IBLOCK_TYPE_ID" => "#IBLOCK_TYPE_ID#",
            "NAME" => "#NAME#",
            "CODE" => "#CODE#",
            "LID"  => #LID#
        );
        if (isset($response['type']) && $response['type'] == "success"){
            return true;
        }
        return false;